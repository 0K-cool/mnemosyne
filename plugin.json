{
  "name": "mnemosyne",
  "version": "2.0.0-alpha",
  "description": "Structured AI memory for professionals. 100% local. Zero cloud. Security-first. v2 adds memory-driven runtime enforcement: memory entries with an `enforce:` block generate Claude Code hooks that hard-block at the tool boundary.",
  "author": "ZerOK Labs",
  "repository": "https://github.com/0K-cool/mnemosyne",
  "license": "MIT",
  "compatibleWith": {
    "claudeCode": ">=2.1.0"
  },
  "commands": [
    {
      "name": "mnemosyne-setup-rag",
      "file": "skills/setup-rag/SKILL.md",
      "description": "Bootstrap 0k-rag for enhanced semantic retrieval (optional)"
    }
  ],
  "features": {
    "mcpServer": false,
    "cliTools": true,
    "pythonLibrary": true,
    "slashCommands": true,
    "hooks": true,
    "agents": false
  },
  "optionalIntegrations": {
    "0k-rag": {
      "description": "Hybrid semantic retrieval (vector + BM25 + BGE reranker). Upgrades recall from ~60% to 100%.",
      "setupCommand": "/mnemosyne-setup-rag",
      "requiredMcpTool": "search_kb"
    }
  }
}
